# Makefile for MPIP	-*-Makefile-*-
# Copyright (C) 2001 Jeffrey Vetter vetter3@llnl.gov
# $Header$

srcdir=@srcdir@
VPATH = $(srcdir)

include ../Defs.mak

CFLAGS	+= 
FFLAGS  += -g 
LDFLAGS	+= -L..
FLIBS	:= -l$(MPIPFLIB) $(LIBS)
LIBS	:= -lmpiP $(LIBS)

CSRCS =	0-simple.c \
	1-hot-potato.c \
	2-pcontrol.c \
	5-medley.c \
	6-nonblock.c \
	7-nonblock-waitall.c \
	8-test.c \
	9-test-mpip-time.c 
FSRCS =	0-simplef.f sweep-ops.f
EXES = $(CSRCS:.c=.exe)
EXES += $(FSRCS:.f=.exe)

all: ${EXES}
	@echo All done.

.SUFFIXES: .exe
.c.exe:
	${CC} ${CFLAGS} ${CPPFLAGS} $< -o $@ ${LDFLAGS} ${LIBS}

.C.exe:
	${CXX} ${CFLAGS} ${CPPFLAGS} $< -o $@ ${LDFLAGS} ${LIBS}

.f.exe:
	${FC} ${FFLAGS} ${CPPFLAGS} $< -o $@ ${LDFLAGS} ${FLIBS}

clean::
	rm -f ${EXES} *.mpiP *.o

include $(srcdir)/../Rules.mak

##### EOF
