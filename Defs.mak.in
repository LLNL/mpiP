# Makefile for MPIP	-*-Makefile-*-
# Copyright (C) 2001 Jeffrey Vetter vetter3@llnl.gov
# $Header$

SHELL	= /bin/sh
MAKE	= make
CC	= @CC@
CXX	= @CXX@
FC	= @F77@
AR	= ar
RANLIB	= @RANLIB@
PYTHON  = python

LIBS        = @LIBS@
LDFLAGS     = @LDFLAGS@
F77_SYMBOLS = @F77_SYMBOLS@

ifneq ( $(srcdir), "." )
CPPFLAGS   += -I.
endif
CPPFLAGS    = -I$(srcdir) @CPPFLAGS@

ifdef FAST
OPT	+= -O3
endif

ifdef NODEBUG
OPT	+= 
else
OPT	+= -g
endif

OS	+= $(shell uname)
ARCH    = $(shell uname -m)

ifeq ($(OS),OSF1)
  LIBS += -lexc
endif

ifeq ($(OS),Linux)
  ifeq ($(ARCH),i686)
    CPPFLAGS += -DIA32
  endif
  ifeq ($(ARCH),alpha)
    CPPFLAGS += -Dalpha
  endif
endif

TARGET	 = libmpiP.a
MPIPLIB  = mpiP
MPIPFLIB = mpiP

#  On Linux, GNU and Intel Fortran compilers require a separate
#  library, due to unique argc/argv symbols.

FORTRAN_TARGET =
F77_IS_GNU = @F77_IS_GNU@

ifeq ($(F77_IS_GNU),true)
  FORTRAN_FLAG = -DGNU_Fortran
  FORTRAN_TARGET = libmpiPg77.a
  MPIPFLIB = mpiPg77
endif

ifneq (,$(findstring ifc, $(FC)))
    FORTRAN_FLAG   = -DIntel_Fortran
    FORTRAN_TARGET = libmpiPifc.a
    MPIPFLIB = mpiPifc
endif


CFLAGS	+= ${OPT}
CPPFLAGS+= -D${OS}
LFLAGS	+=
LIBS	+=

##### EOF
